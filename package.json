{
  "name": "playground",
  "version": "0.0.0",
  "description": "demo",
  "main": "app.js",
  "scripts": {
    "dev": "npx run-p dev:*",
    "dev:server": "npx nodemon dist/main.js --quiet",
    "dev:build": "npx tsc -p config/ -w",
    "test": "npx jest --config=config/jest.config.js"
  },
  "keywords": [
    "the netz"
  ],
  "author": "Nat Hamilton",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/jest": "^23.3.10",
    "jest": "^23.6.0",
    "nodemon": "^1.18.7",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^23.10.5",
    "typescript": "^3.2.2"
  }
}
